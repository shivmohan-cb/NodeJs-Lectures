<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ejs Templating Engine</title>
</head>
<body>
    <header>
        <a href="/about"><%= headerData[0] %></a>
        <a href="/contact"><%= headerData[1] %></a>
    </header>
    <h1>This is EJS Rendered Page</h1>
    <% for(let i=0;i<list.length;i++) { %>
       <h2><%= list[i].title %></h2>
       <p><%= list[i].desc %></p> 
    <% } %>
</body>
</html>